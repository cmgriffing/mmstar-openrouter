{
  "name": "mmstar-openrouter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.24.0",
  "engines": {
    "node": ">=24.16.0",
    "pnpm": ">=10.24.0"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check": "turbo run typecheck lint test",
    "format": "biome format --write .",
    "validate": "turbo run validate",
    "benchmark": "turbo run benchmark",
    "resume": "turbo run resume",
    "retry-failed": "turbo run retry-failed",
    "restart": "turbo run restart",
    "export": "turbo run export"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "turbo": "catalog:",
    "typescript": "catalog:"
  }
}
